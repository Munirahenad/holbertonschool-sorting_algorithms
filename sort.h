#ifndef SORT_H
#define SORT_H

#include <stddef.h>

/* Printing helpers */
void print_array(const int *array, size_t size);

/* Sorting algorithms */
void merge_sort(int *array, size_t size);

#endif /* SORT_H */
